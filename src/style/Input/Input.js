import styled from 'styled-components';
import { Input as InputForm} from '@rebass/forms'

const Input = styled(InputForm)`
  color: #000;
  margin-bottom: 10px;
`;

export default Input;
export { Input };
